
export type userParamURIModel = {
    id: string
}

export type userInputModel = {
    login: string,
    password: string,
    email: string,
}

export type userAuthModel = {
    loginOrEmail: string,
    password: string
}